// TypeScript type-resolution stub.
// Metro resolves reminders.native.ts on native and reminders.web.ts on web at build time.
export { requestNotificationPermission, syncReminders } from './reminders.web';
